export interface TextIndex {
    start: number;
    end: number;
    children: {
        [key: string]: TextIndex;
    };
}
export declare class TextIndexer {
    _filename: string;
    _keyfunc: (line: string) => string;
    _stacksize: number;
    _index: Promise<TextIndex>;
    constructor(filename: string, keyfunc: (line: string) => string, stacksize: number);
    index(): Promise<TextIndex>;
    lookup(key: string): Promise<TextIndex | null>;
}
